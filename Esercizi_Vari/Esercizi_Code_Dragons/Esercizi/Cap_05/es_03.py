'''
evoca `sum_multiples(limit)` per restituire la somma di tutti i numeri 
**minori di** `limit` divisibili per `3` **oppure** `5`. Se `limit` ≤ `0`, torna `0`
'''

def sum_multiples(limit: int) -> int:
    ...